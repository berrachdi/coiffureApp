<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mycoiffeur">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyCoiffeur"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".interfaces.CompleteProfile"
            android:exported="false" />
        <activity android:name=".interfaces.FileActiviteScreen" />
        <activity
            android:name=".interfaces.ProfileActivity"
            android:theme="@style/fullScreen" />
        <activity android:name=".interfaces.HomeScreenActivity">

        </activity>
        <activity android:name=".interfaces.RegisterScreenActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".interfaces.LoginScreenActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>